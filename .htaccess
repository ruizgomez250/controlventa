Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Abrir el acceso web desde /controlventa/.
    RewriteRule ^$ /controlventa/public/login [R=302,L]

    RewriteCond %{ENV:REDIRECT_STATUS} ^$
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
